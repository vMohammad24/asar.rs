[package]
name = "asar-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
asar = { path = ".." }
clap = { version = "4", features = ["derive"] }
walkdir = "2"

[[bin]]
name = "asar"
path = "src/main.rs"
